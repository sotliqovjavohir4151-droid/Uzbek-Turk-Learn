## Packages
framer-motion | Smooth animations for page transitions and micro-interactions
lucide-react | Beautiful icons for the UI
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes

## Notes
Using mock data for initial development as per requirements.
Audio playback uses browser's SpeechSynthesis API (no external package needed).
